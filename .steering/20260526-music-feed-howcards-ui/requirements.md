# Music Feed How Cards UI Requirements

- Artist detail must fetch How cards for the same Firestore `song_id` that produced the Home dashboard item.
- Home artist cards must keep overlay controls visually inside the rounded card.
- iOS must stop seeding `users` documents on app launch and while reading How cards.
- Existing unrelated `project.pbxproj` changes must remain untouched.

